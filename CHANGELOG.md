# Changelog

## v0.2.0 (2026-09-16)

### 新增

- **临时分享**：通过 Cloudflare Quick Tunnel 分享只读笔记快照；解密密钥只放在链接 `#` 之后
- **图表文档**：基于 diagrams.net 的 Draw.io 编辑器
- **回收站**：文档移到仓库 `.trash` 目录，资源文件保留
- **中英界面**：账户菜单切换语言，登录页也可切换
- **账户菜单**：主题、语言、反馈 Issue、给仓库点 Star、退出登录

### 改进

- 侧栏顶栏改为同步状态和上次同步时间，同步按钮放在新建旁边
- 窗口标题显示登录来源，例如 `UNote-Gitee`
- 新建菜单支持笔记、画布、脑图、图表和文件夹
- 去掉 notebooks.json 元数据层，直接以仓库目录为工作区
- 首次分享 cloudflared 时先走 gh-proxy.com，失败再直连 GitHub，并校验官方 SHA-256

### 平台

- Windows（NSIS 安装包）

## v0.1.0 (2026-09-15)

### 首次发布

UNote — Git 优先的轻量桌面文档应用。

### 功能

- **Markdown 编辑**：基于 MDXEditor 的富文本编辑，支持 GFM 语法
- **画布文档**：基于 Excalidraw 的自由绘图
- **脑图**：基于 Mind Elixir 的节点式思维导图，支持添加/删除/拖拽节点
- **文件夹管理**：支持任意嵌套目录结构，新建/重命名/删除
- **Git 同步**：自动 commit + push，支持多设备同步
- **资源管理**：图片等附件存储在 `.assets/` 目录，文档可直接引用
- **Gitee 登录**：OAuth 2.0 认证，凭据存储在系统 Keyring
- **自动更新**：应用启动后自动检查新版本，一键更新并重启
- **暗色模式**：跟随系统或手动切换
- **自定义 App Icon**：shadcn 风格，深色底 + 白色文档 + 绿色枝叶

### 平台

- Windows（NSIS 安装包 + 便携版）

> macOS 和 Linux 支持将在后续版本中提供。
